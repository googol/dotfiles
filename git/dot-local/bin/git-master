#!/bin/bash

set -euo pipefail

main () {
  local -r main_branch="$(git detect-main-branch)"

  if [[ -z "$main_branch" ]]; then
    exit 1
  fi

  git checkout "$main_branch"
  git pull --rebase
  git bc "$main_branch"
}

main "$@"
